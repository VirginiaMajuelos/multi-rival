<section id="contact">
  <div class="contact-container__header">
    <h2>CONTÁCTANOS</h2>
    <hr />
    <p>
      Si deseas obtener más información sobre nuestros servicios de construcción
      o si tienes alguna pregunta, no dudes en contactarnos. Estamos disponibles
      para responder tus preguntas y ayudarte en lo que necesites.
    </p>
  </div>
  <div class="contact-container__form">
    <form>
      <div class="form-row">
        <div class="form-group">
          <label for="name">Nombre:</label>
          <input
            type="text"
            id="name"
            name="name"
            placeholder="Ingresa tu nombre"
          />
        </div>
        <div class="form-group">
          <label for="email">Correo:</label>
          <input
            type="email"
            id="email"
            name="email"
            placeholder="Ingresa tu correo"
          />
        </div>
      </div>
      <div class="form-row">
        <div class="form-group full-width">
          <label for="message">Mensaje:</label>
          <textarea
            id="message"
            name="message"
            rows="5"
            placeholder="Ingresa tu mensaje"></textarea>
        </div>
      </div>
      <div class="form-row">
        <button type="submit" disabled>Enviar</button>
      </div>
    </form>
  </div>
</section>

<style>
  section {
    background-color: whitesmoke;
    text-align: center;
    height: 740px;
  }

  .contact-container__header {
    display: flex;
    flex-direction: column;
    align-items: center;
    padding: 40px 80px;
  }

  .contact-container__header h2 {
    margin: 0 0 20px 0;
    font-weight: 500;
    font-size: 34px;
    color: #333;
  }

  .contact-container__header p {
    font-size: 22px;
    color: #777;
    padding: 20px 70px;
  }
  .form-row {
    margin-bottom: 10px;
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .form-group {
    display: inline-block;
    margin-right: 10px;
  }

  .form-group label {
    display: block;
    margin-bottom: 5px;
  }

  .form-group input {
    padding: 10px;
    border: 1px solid #ccc;
    border-radius: 4px;
    width: 400px;
  }
  .form-group textarea {
    padding: 10px;
    border: 1px solid #ccc;
    border-radius: 4px;
    width: 810px;
  }

  .form-group .full-width {
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
  }

  button[type="submit"] {
    text-transform: uppercase;
    color: #fff;
    background-color: #c92d2d;
    border: 0;
    padding: 14px 20px;
    margin: 0;
    font-size: 16px;
    font-weight: 500;
    letter-spacing: 0.5px;
    border-radius: 0;
    margin-top: 20px;
    transition: all 0.5s;
    opacity: 0.5;
  }
</style>
